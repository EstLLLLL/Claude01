# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository (`EstLLLLL/Claude01`) is currently a scaffold with no source code. It contains only a `README.md` with the project name `Claude01` and no build, test, lint, or runtime tooling configured.

There is no detectable language, framework, package manager, or architecture yet. Do not assume any — when adding the first code, choose tooling based on the task the user describes and update this file with the resulting conventions.

## Workflow Conventions

- Active development branch for documentation work: `claude/add-claude-documentation-J0P5n`. Do not push to other branches without explicit permission.
- GitHub interactions go through the `mcp__github__*` MCP tools; the `gh` CLI is not available.
- Only this repository (`EstLLLLL/Claude01`) is in scope for GitHub MCP calls.

## When This File Becomes Stale

As soon as real code lands, replace this section with:
1. The actual build / test / lint / run commands (including how to run a single test).
2. The high-level architecture — only the cross-file "big picture" that isn't obvious from reading one file.

Avoid restating generic development practices or enumerating files that are trivially discoverable.
